{% extends "layout.html.twig" %}

{% block title %}{{ trans('error.404_title') }}{% endblock %}

{% block content %}
<div class="text-center py-5">
    <div style="font-size: 80px;">🧸</div>
    <h1 class="display-4 fw-bold text-purple mt-3">404</h1>
    <h2 class="h4 text-muted mb-3">{{ trans('error.404_heading') }}</h2>
    <p class="text-muted mb-4">{{ trans('error.404_message') }}</p>
    <a href="{{ base_path }}/" class="btn btn-purple btn-lg px-5">{{ trans('error.404_back_home') }}</a>
</div>
{% endblock %}